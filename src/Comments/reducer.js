import { ADD_COMMENT, VOTE_COMMENT } from './actions'

function posts (state = [], action) {
  switch(action.type) {
    case ADD_COMMENT :

    case ADD_COMMENT :

    default :
      return state;
  }
}

export default posts
